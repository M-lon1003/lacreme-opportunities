// Placeholder E2E test for saving and handing off an opportunity
describe('Save and handoff', () => {
  it('allows the user to save an opportunity and queue it for grant writer', () => {
    // In a full e2e suite, we would navigate to /app, click Save and Send
    // and verify the payload shape posted to /api/grant-writer/queue.
    expect(true).toBe(true)
  })
})